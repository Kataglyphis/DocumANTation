% Profile: default -- the general-purpose CV published on jonasheinle.de, in
% both languages. This is the section set cv.tex carried inline before profiles
% existed; the five sections left out are left out to fit one page, not because
% they are stale. Keep this profile's output stable: the two PDFs it builds are
% the pair linked from jonasheinle.de.

% The tagline used to name the job and stop there. The second half costs
% nothing -- the line was half empty -- and it is the only place above the fold
% that says how I work rather than where. "Maintainer" is meant literally: the
% repositories behind the GitHub link in the header, this document pipeline
% among them, are mine to keep working.
\newcommand{\cvTagline}{\IfLanguageName{english}{Research Associate @ Fraunhofer IPA | Open source maintainer \& contributor}{Wissenschaftlicher Mitarbeiter @ Fraunhofer IPA | Open-Source-Maintainer \& -Contributor}}

% The summary above the timeline is new, and one page is a hard constraint,
% so it has to be paid for. The Gymnasium entry is what buys it: with two KIT
% degrees on the page it is the least informative row, and it is the same
% trade mistral-rse makes. One line to reverse -- delete the flag below.
\cvshowschoolfalse
% ... and the thesis lines. The Fraunhofer entry grew by four lines when the
% GLM-5.2 serving work went in, and this is the cheapest place to find them:
% the flag drops *detail* only -- both degrees keep their dates, grades and
% subject tags, so the timeline has no hole. Two lines in English, three in
% German, where the Masterarbeit title runs onto a second line by itself.
\cvshowthesislinesfalse


\newcommand{\cvBody}{%
    \input{section_headline}%
    % Trimmed to pay for the larger gap after the tagline above: the space is
    % redistributed inside the header rather than added to the page.
    \vspace{0.25cm}%
    \input{section_experience}%
    \input{section_education}%
    % Left out to fit the CV on one page -- add back here, not in cv.tex.
    % \input{section_skills}%
    % \input{section_projects}%
    % \input{section_teaching_mentoring}%
    \input{section_private}%
    % \input{section_international_experiences_volunteering}%
    % \input{section_extracurricular_courses_certificates}%
    % section_references stays out by default: switch it on for applications
    % that ask for referees.
    % \input{section_references}%
}
